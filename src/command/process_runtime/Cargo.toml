[package]
name = "ovid-command-process"
version = "0.1.0"
edition = "2024"
description = "Command process-tree primitives for ovid-native"
license = "MIT"
publish = false

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "=0.61.2", features = [
    "Win32_Foundation",
    "Win32_System_Diagnostics_ToolHelp",
    "Win32_System_Threading",
] }
